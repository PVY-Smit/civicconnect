# Template only. Real values are shared outside this repository.
# Copy to .env locally. .env is git-ignored and must never be committed.
# See CONTRIBUTING.md, Secrets.

# DATABASE_CONNECTION=
# APP_ENVIRONMENT=Development
